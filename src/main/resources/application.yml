server:
  port: 8889
  servlet:
    context-path: /

#数据源的配置
spring:
  profiles:
    active: dev  #使用的配置文件是开发环境的
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jmx:
    default-domain: CodeDoamin